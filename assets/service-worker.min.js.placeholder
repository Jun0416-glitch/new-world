/* Placeholder for vendor service-worker.min.js
 * Source: https://3nbf4.com/act/files/service-worker.min.js?r=sw
 *
 * This placeholder file indicates where the third-party service-worker script
 * should be placed once fetched and reviewed. It is intentionally empty so it
 * will not register or execute any service worker logic until replaced with the
 * real, audited script content.
 *
 * To replace:
 * 1. Fetch the real file: curl -L 'https://3nbf4.com/act/files/service-worker.min.js?r=sw' -o assets/service-worker.min.js
 * 2. Review the contents for safety and remove this .placeholder extension (or replace the file in-place).
 * 3. Commit the real file and deploy.
 */

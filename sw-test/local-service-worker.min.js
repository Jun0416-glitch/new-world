// local-service-worker.min.js - SAFE stub for testing importScripts integration
console.log('local-service-worker stub loaded');
self.addEventListener('message', function(ev) {
  // stub: echo message
  if (ev && ev.data && ev.source) {
    console.log('local-service-worker received message', ev.data);
  }
});
